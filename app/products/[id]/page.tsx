import {notFound} from "next/navigation"; import {AddToCart} from "@/components/add-to-cart"; import {products} from "@/lib/products"; export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const product=products.find(p=>p.id===id);if(!product)notFound();return <div className="container detail"><div className="image big">{product.emoji}</div><div><p className="eyebrow">{product.category}</p><h1>{product.name}</h1><h2>${product.price.toFixed(2)}</h2><p>{product.description}</p><AddToCart product={product}/></div></div>}