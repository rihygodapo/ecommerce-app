import Link from "next/link"; import type {Product} from "@/lib/products"; import {AddToCart} from "./add-to-cart"; export function ProductCard({product}:{product:Product}){return <article><Link href={"/products/"+product.id} className="image">{product.emoji}</Link><p className="eyebrow">{product.category}</p><Link href={"/products/"+product.id}><h3>{product.name}</h3></Link><div className="footer"><b>${product.price.toFixed(2)}</b><AddToCart product={product}/></div></article>}